import styled from 'styled-components';
import { adminInputStyles } from '../AdminInputField/UiComponents';

export const AdminTextArea = styled.textarea`
    ${adminInputStyles};
    margin: 0;
    resize: vertical;
`;
